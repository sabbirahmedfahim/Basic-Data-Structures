#include <bits/stdc++.h> //goto create_node2.cpp
using namespace std;
// at first recap Pointer's concept
int main()
{
    int n = 10;
    int *ptr = &n;
    cout << *ptr;

    //our goal is to make an data type(say int) & it's Pointer at once
    // since it is not possible manually, we need the next of class 
    

    return 0;
}